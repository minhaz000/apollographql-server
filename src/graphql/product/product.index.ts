import typeDef from "./product";
import { queries } from "./product.queries";
import { mutations } from "./product.mutations";
import { resolvers } from "./product.resolvers";

export const product = { typeDef, queries, mutations, resolvers };
