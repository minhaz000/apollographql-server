import typeDef from "./post";
import { queries } from "./post.queries";
import { mutations } from "./post.mutations";
import { resolvers } from "./post.resolvers";

export const Post = { typeDef, queries, mutations, resolvers };
