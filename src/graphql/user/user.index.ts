import typeDef from "./user";
import { queries } from "./user.queries";
import { mutations } from "./user.mutations";
import { resolvers } from "./user.resolvers";

export const User = { typeDef, queries, mutations, resolvers };
